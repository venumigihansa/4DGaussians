# Install the official SAM2 package separately, then install these preprocessing helpers.
numpy>=1.24
opencv-python>=4.8
Pillow>=9.0
torchvision>=0.14  # RAFT support; install a build compatible with the existing torch.
